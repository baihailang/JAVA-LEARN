##   jvm调优的目的

减少stw   减少stop work时间  因为频繁gc 会停止工作


## jvm 内存分析
![[Pasted image 20260826205914.png]]


`Math.java` 被编译成 `.class`，类加载子系统将其放入**方法区**；随后 `main` 线程启动，在**虚拟机栈**中依次压入 `main` 和 `compute` 栈帧，`compute` 栈帧通过**局部变量表**存储 `a=1，b=2`，并通过**程序计数器=4**定位当前执行位置，**执行引擎**负责驱动这一切计算，计算结果可能返回给 `main`



**1、堆（Heap）：** 这是Java内存管理中最大的一块，被所有线程共享。在堆中主要存放对象实例和数组。

**2、栈（Stack）：** 每个线程运行时都会创建一个栈，用于存放局部变量、操作数栈、动态链接和方法出口等。栈的生命周期和线程同步。

**3、方法区（Method Area）：** 同样被所有线程共享，用于存储已被虚拟机加载的类信息、常量、静态变量等数据。

**4、本地方法栈（Native Method Stack）：** 专门用于处理本地native方法的调用。

此外，Java内存模型还包括程序计数器等组件，共同构成了Java的运行时数据区。


![[Pasted image 20260826213156.png]]

垃圾回收由代码执行引擎执行

### 1. 年轻代的内部结构（从左到右）

图中明确标出了年轻代的三个区域及其**默认内存比例**（可通过 `-XX:SurvivorRatio` 调整）：

- **Eden区（伊甸园）**：占年轻代的 **8/10**（80%）。绝大多数新创建的对象都会在这里分配内存。
    
- **S0（Survivor 0）**：占 **1/10**（10%）。
    
- **ST（这里应为S1或To Survivor）**：占 **1/10**（10%）。
    

> **核心机制**：S0和S1（两个Survivor区）在任意时刻，**必有一个是空的**（作为“To”区），用来存放下一次Minor GC后存活下来的对象。

### 2. Minor GC 的执行流程（复制算法）

图中的 **`minor gc`** 箭头指向了对象在年轻代内部的流动方向，流程如下：

1. **新对象**总是先分配到 **Eden** 区。
    
2. 当Eden区空间不足时，触发 **Minor GC**（也称Young GC）。
    
3. JVM采用**标记-复制**算法：将 **Eden区** 和 **当前非空的Survivor区（假设为S0）** 中**存活**的对象，一次性复制到 **另一个空的Survivor区（ST/S1）**。
    
4. 复制完成后，**清空** Eden区和原来的S0区。
    
5. 此时，**S1** 变成非空（存放存活对象），而 **S0** 变成空区，等待下一次Minor GC时作为接收区。**两个Survivor区（S0和S1）的角色就此互换**。
    

### 3. 关键数值“15”的含义（晋升阈值）

图中在Survivor区标注的 **“15”**，代表**对象年龄阈值**（即 `-XX:MaxTenuringThreshold` 默认值）。

- 每经历一次Minor GC且存活下来，对象的“年龄”就 **+1**。
    
- 当对象的年龄达到 **15** 时，它就不再适合待在年轻代了，会在下一次Minor GC时被**晋升（Promoted）** 到**老年代（Old Generation）**。
    

### 4. Full GC 和 OOM 的触发链条

图中的 **`full gc`** 和 **`OOM`** 连接了年轻代与整个堆的“生死线”：

- 当大量对象在Minor GC后存活，导致Survivor区放不下时，**多余的对象会直接提前晋升到老年代**。
    
- 如果老年代空间也不足，就会触发 **Full GC**（对整个堆和方法区进行全局回收）。
    
- 如果 **Full GC 执行后**，老年代依然无法容纳晋升进来的对象，或者堆内存整体无法满足新对象的分配需求，JVM最终就会抛出 **OOM（OutOfMemoryError）**
**一句话总结图中的逻辑流向**：  
对象先在 **Eden (8/10)** 出生，经历 **Minor GC** 后，存活者借助 **复制算法** 在 **S0** 和 **ST/S1 (各1/10)** 之间反复拷贝并累加年龄，达到 **15岁** 后晋升老年代；若整个过程中老年代也满了，就会触发 **Full GC**，最终无法释放空间时抛出 **OOM**。


# Arthas 常用命令速查手册

Arthas 是阿里巴巴开源的 Java 诊断工具，能在不重启应用的情况下实时监控、排查线上问题。以下为最常用命令的分类说明、作用及典型用法示例。

---

## 1. 基础命令

| 命令 | 作用 | 示例 |
|------|------|------|
| `help` | 查看所有命令列表或指定命令的详细用法 | `help`<br>`help dashboard` |
| `dashboard` | 实时展示 CPU、内存、线程、GC 等核心指标 | `dashboard -i 2000`（每 2 秒刷新） |
| `thread` | 查看线程状态、CPU 占比、堆栈，定位死锁/阻塞 | `thread -n 5`（最忙 5 个线程）<br>`thread -b`（检测死锁） |
| `jvm` | 查看当前 JVM 的详细信息（内存区域、GC 策略等） | `jvm` |
| `quit` / `exit` | 退出当前 Arthas 会话，不影响目标应用 | `quit` |
| `reset` | 重置被 Arthas 增强过的类，还原为原始状态 | `reset`（重置所有）<br>`reset *Service` |

---

## 2. 类与方法查询

| 命令          | 作用                       | 示例                                                                    |
| ----------- | ------------------------ | --------------------------------------------------------------------- |
| `sc`        | 搜索 JVM 中已加载的类信息          | `sc -d com.example.UserService`                                       |
| `sm`        | 查看类的方法签名                 | `sm -d com.example.UserService`                                       |
| `jad`       | 反编译指定已加载类的源码，验证代码是否生效    | `jad java.lang.String`<br>`jad --source-only com.example.UserService` |
| `getstatic` | 查看类的静态属性值                | `getstatic demo.MathGame random`                                      |
| `ognl`      | 动态执行 OGNL 表达式，可调用方法、访问属性 | `ognl --classLoaderClass org... '@demo.MathGame@random.nextInt(100)'` |

---

## 3. 方法监控与诊断（最常用）

| 命令        | 作用                             | 示例                                                                                    |
| --------- | ------------------------------ | ------------------------------------------------------------------------------------- |
| `watch`   | 监控方法的**入参、返回值、异常**，支持条件过滤      | `watch com.example.UserService getUserById "{params, returnObj, throwExp}" -x 3 -n 5` |
| `trace`   | 追踪方法**内部调用路径**及每个节点耗时，定位性能瓶颈   | `trace com.example.UserService getUserById '#cost>10' -n 5`                           |
| `stack`   | 输出方法的**调用路径**（向上追溯，谁调用了该方法）    | `stack com.example.UserService getUserById -n 5`                                      |
| `monitor` | 方法调用统计（次数、成功率、平均耗时等）           | `monitor -c 60 com.example.UserService getUserById`                                   |
| `tt`      | 方法调用**时空隧道**，记录每次调用的完整上下文，支持回放 | `tt -t com.example.UserService getUserById`<br>`tt -i 1000 -p`（回放编号 1000 的调用）         |

> **常用参数说明**：
> - `-n`：限制执行次数
> - `-x`：展开层级深度（如 `-x 3`）
> - `'#cost>10'`：过滤耗时大于 10ms 的调用
> - `-b`：方法执行**前**观测（默认是执行后）
> - `-e`：观测异常情况

---

## 4. JVM 与系统诊断

| 命令         | 作用                      | 示例                                                |
| ---------- | ----------------------- | ------------------------------------------------- |
| `heapdump` | 导出堆内存快照，用于内存分析          | `heapdump /tmp/dump.hprof`                        |
| `logger`   | 查看和动态修改日志级别             | `logger --name com.example.MyClass --level warn`  |
| `sysprop`  | 查看和修改 JVM 系统属性          | `sysprop`<br>`sysprop java.version`               |
| `memory`   | 查看 JVM 内存使用情况（堆、非堆、各区域） | `memory`                                          |
| `profiler` | 生成 CPU 火焰图，分析性能热点       | `profiler start`<br>`profiler stop --format html` |
|            |                         |                                                   |

---

## 5. 代码热更新

| 命令                           | 组合作用                               | 示例                                                                                                                                                                                    |
| ---------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `jad` + `mc` + `retransform` | **三步走**热更新：反编译 → 修改 → 编译 → 加载      | 1. `jad --source-only com.example.UserService > /tmp/UserService.java`<br>2. （手动修改源码）<br>3. `mc /tmp/UserService.java -d /tmp`<br>4. `retransform /tmp/com/example/UserService.class` |
| `redefine`                   | 直接热加载 .class 文件（需先 `reset` 消除监控影响） | `redefine /tmp/UserService.class`                                                                                                                                                     |

> ⚠️ **注意**：`redefine` 与 `watch`/`trace` 等监控命令冲突，执行前应先 `reset` 清除增强。

---

## 6. 快速上手指南
### 6.1 安装与启动
```bash
# 下载 arthas-boot.jar
curl -O https://arthas.aliyun.com/arthas-boot.jar
# 启动并 attach 到目标 Java 进程（输入进程编号）
java -jar arthas-boot.jar
```



# JVM 内存分配与调优指南

JVM 调优不是简单的“加内存”或“改参数”，它需要结合业务特点、系统资源和监控数据，有策略地进行。

---

## 1. 调优目标：在“三难困境”中找平衡

JVM 调优的本质，是在以下三个目标之间寻找最适合你业务的平衡点：

| 目标                           | 说明                       | 适用场景                          |
| :--------------------------- | :----------------------- | :---------------------------- |
| **低延迟 (Low Latency)**        | 追求更短的 GC 停顿时间（如 < 100ms） | 对响应时间敏感的**实时交互**场景（电商秒杀、支付交易） |
| **高吞吐量 (High Throughput)**   | 追求单位时间内完成更多任务            | **后台计算**场景（批处理、报表分析）          |
| **内存高效 (Memory Efficiency)** | 合理利用内存，避免浪费并防止内存溢出（OOM）  | 资源受限环境                        |
|                              |                          |                               |

> **注意**：通常，低延迟和高吞吐量难以两全，需要根据业务确定优先级。

---

## 2. 核心战场：JVM 内存模型概览

调参前，先了解 JVM 内存的主要区域：

| 区域                                   | 说明                           | 是否调优重点 |
| :----------------------------------- | :--------------------------- | :----: |
| **堆内存 (Heap)**                       | 存储所有**对象实例**。调优的核心区域         |   ✅    |
| **元空间 (Metaspace)**                  | 存储**类信息**、常量池等（Java 8 取代永久代） |   ✅    |
| **虚拟机栈 (VM Stack)**                  | 每个线程私有，存储方法调用的**栈帧**         | 一般无需调优 |
| **本地方法栈 (Native Method Stack)**      | 为 `native` 方法服务              | 一般无需调优 |
| **程序计数器 (Program Counter Register)** | 记录线程执行位置                     |  无需调优  |

---

## 3. 调优工具箱：核心参数详解

### 3.1 堆内存大小：最重要的基础参数

| 参数 | 说明 | 建议 |
| :--- | :--- | :--- |
| **`-Xms`** | 设置堆内存的**初始**大小 | **建议与 `-Xmx` 设置为相同值**，避免 JVM 运行时动态调整堆大小带来的性能开销 |
| **`-Xmx`** | 设置堆内存的**最大**大小 | 生产环境务必显式设置 |
| **`-Xmn`** | 设置**年轻代**（Young Generation）的大小 | 官方推荐为整个堆大小的 **3/8** |

> **关于堆大小的建议**：
> - **总量规划**：通常建议 JVM 堆内存占服务器总内存的 **60% ~ 80%**。例如，一台 4G 内存的服务器，可以给 JVM 分配 **3G**（`-Xms3072m -Xmx3072m`）。
> - **不要过大**：堆内存过大会导致一次 Full GC 时间过长，影响服务可用性。

**参考值（需结合压测调整）**：

| 服务器配置 | `-Xms` / `-Xmx` | `-Xmn` (年轻代) | `-XX:MetaspaceSize` |
| :--- | :--- | :--- | :--- |
| 1C 2G | 1G | 500M | 128M |
| 2C 4G | 2560M | 1200M | 256M |
| 4C 8G | 4G | 2G | 384M |
| 8C 16G | 10G | 5G | 512M |

### 3.2 分代比例：精细化管理内存

年轻代和老年代的比例，直接影响着 Minor GC 和 Full GC 的频率。

| 参数 | 说明 | 默认值 | 示例 |
| :--- | :--- | :--- | :--- |
| **`-XX:NewRatio`** | 老年代 : 年轻代的**比值** | 2（老年代:年轻代 = 2:1，年轻代占堆的 1/3） | `-XX:NewRatio=4`（年轻代占堆的 1/5） |
| **`-XX:SurvivorRatio`** | Eden 区 : 一个 Survivor 区的**比值** | 8（Eden:S0:S1 = 8:1:1） | `-XX:SurvivorRatio=4`（Eden:S0:S1 = 4:1:1） |
| **`-XX:MaxTenuringThreshold`** | 对象在年轻代经历多少次 Minor GC 后晋升到老年代 | 15 (Parallel GC) / 6 (CMS GC) | 值越小，对象越早晋升到老年代 |

### 3.3 非堆内存与线程栈

| 参数 | 说明 | 建议 |
| :--- | :--- | :--- |
| **`-XX:MetaspaceSize`** | 元空间的**初始**大小 | 根据应用加载的类数量设置，避免启动时频繁扩容 |
| **`-XX:MaxMetaspaceSize`** | 元空间的**最大**大小 | **建议设置上限**，防止因类加载问题导致元空间无限膨胀 |
| **`-Xss`** | 设置每个**线程栈**的大小 | JDK 8+ 默认为 1M。如果应用线程数非常多，可适当减小（如 256k） |

---

## 4. 垃圾收集器（GC）选型

选择合适的 GC 是调优的关键一步。

| 收集器 | 适用场景 | 关键参数 |
| :--- | :--- | :--- |
| **G1 (`-XX:+UseG1GC`)** | **多核大内存**（堆 > 6GB），追求**低延迟**和**可预测停顿**的**通用场景** | `-XX:MaxGCPauseMillis=200`（目标停顿时间） |
| **Parallel (`-XX:+UseParallelGC`)** | **多核 CPU**，追求**高吞吐量**的后台任务，允许较长 GC 停顿 | JVM 8 的默认 GC |
| **CMS (`-XX:+UseConcMarkSweepGC`)** | **多核 CPU**，对**延迟**要求高，但内存小于 4G 的场景 | 已在 Java 9 中标记为废弃 |
| **Serial (`-XX:+UseSerialGC`)** | 单核 CPU 或内存小于 100M 的**极小应用** | - |

---

## 5. 调优实战流程：一套可复用的方法

1. **明确目标**：确定业务是优先低延迟还是高吞吐量。
2. **建立基准**：在测试环境用**默认参数**运行，通过监控工具记录 GC 频率、停顿时间等作为“基线”。
3. **设定参数**：根据服务器内存设置 `-Xms` 和 `-Xmx`，并选择一个 GC（推荐 G1）。
4. **压力测试**：使用 JMeter 等工具模拟真实流量，观察应用表现。
5. **监控与分析**：利用工具观察 GC 日志和内存使用，找出问题。
6. **微调参数**：根据分析结果，调整分代比例、GC 参数等。
7. **重复验证**：重复压测和调整，直到达到目标。
8. **灰度发布**：在生产环境先对少量机器应用新参数，观察稳定后全量。

---

## 6. 调优利器：常用监控工具

| 工具 | 用途 | 示例命令 |
| :--- | :--- | :--- |
| **`jstat`** | 命令行工具，实时查看 GC 和内存使用情况 | `jstat -gcutil <pid> 1000`（每秒打印一次 GC 概况） |
| **`jmap`** | 生成堆转储（Heap Dump）文件，用于离线分析内存泄漏 | `jmap -dump:live,file=heap.hprof <pid>` |
| **VisualVM / JConsole** | 图形化工具，提供 CPU、内存、线程的实时监控 | 界面操作，无需命令行 |
| **GC 日志** | 开启 GC 日志是分析问题的**最基本要求** | 参见下方参数 |

**GC 日志常用参数**：
```bash
-Xloggc:/path/to/gc.log      # 指定 GC 日志路径
-XX:+PrintGCDetails          # 打印 GC 详细信息
-XX:+PrintGCDateStamps       # 打印 GC 发生的时间戳
```




# JVM 垃圾回收（GC）方式详解

JVM的垃圾回收（GC）方式可以从两个层面来理解：底层的**回收算法**和上层的**垃圾收集器**。算法是理论，定义了“如何回收”；收集器是实践，是算法的具体实现，决定了回收时的性能表现。

---

## 1. 分代收集理论

现代JVM普遍采用**分代收集**策略，根据对象存活周期的不同，将堆内存划分为不同区域，并对每个区域采用最合适的算法。

### 堆内存划分

| 区域 | 特点 | 适用算法 |
| :--- | :--- | :--- |
| **新生代 (Young Generation)** | 对象“朝生夕死”，存活率低 | **复制算法** |
| **老年代 (Old Generation)** | 对象存活率高，生命周期长 | **标记-清除** 或 **标记-整理** |

---

## 2. 核心回收算法

### 2.1 标记-清除 (Mark-Sweep)

| 属性       | 说明                         |
| :------- | :------------------------- |
| **步骤**   | ① 标记所有存活对象 → ② 清除所有未被标记的对象 |
| **优点**   | 速度快，实现简单                   |
| **缺点**   | 产生内存碎片，可能导致频繁GC            |
| **适用区域** | 老年代                        |

### 2.2 复制算法 (Copying)

| 属性       | 说明                                    |
| :------- | :------------------------------------ |
| **步骤**   | 将内存分为两块，只使用一块。GC时将存活对象复制到另一块，一次性清理原区域 |
| **优点**   | 效率高，无内存碎片                             |
| **缺点**   | 内存利用率低（始终有一块空闲）                       |
| **适用区域** | 新生代                                   |

### 2.3 标记-整理 (Mark-Compact)

| 属性       | 说明                                   |
| :------- | :----------------------------------- |
| **步骤**   | ① 标记存活对象 → ② 将存活对象向一端移动 → ③ 清理边界外的内存 |
| **优点**   | 解决了碎片问题                              |
| **缺点**   | 效率比标记-清除稍低                           |
| **适用区域** | 老年代                                  |

---

## 3. 主流垃圾收集器对比

垃圾收集器是上述算法在不同场景下的具体实现。

| 收集器                   | 所属年代 | 算法           | 线程模型  | 特点与适用场景                                                         |
| :-------------------- | :--- | :----------- | :---- | :-------------------------------------------------------------- |
| **Serial**            | 新生代  | 复制算法         | 单线程   | GC时会**暂停所有应用线程（STW）**。适用于**单核CPU或内存很小**的环境                      |
| **Serial Old**        | 老年代  | 标记-整理        | 单线程   | Serial的老年代版本                                                    |
| **ParNew**            | 新生代  | 复制算法         | 多线程   | Serial的**多线程**版本，常与CMS搭配使用                                      |
| **Parallel Scavenge** | 新生代  | 复制算法         | 多线程   | **注重吞吐量**，适用于后台计算场景                                             |
| **Parallel Old**      | 老年代  | 标记-整理        | 多线程   | Parallel Scavenge的老年代版本                                         |
| **CMS**               | 老年代  | 标记-清除        | 并发    | **注重低延迟**，GC停顿短。会产生碎片，**Java 9已废弃**。适用于**内存<4G、响应敏感**的应用        |
| **G1**                | 不分代  | 整体标记-整理，局部复制 | 并发+并行 | 将堆划分为多个**Region**，**优先回收垃圾最多的区域**。可预测停顿，适用于**堆>6GB、追求低延迟**的通用场景 |
| **ZGC**               | 不分代  | 着色指针、读屏障     | 并发    | **超低延迟**，停顿<10ms。适用于**超大堆（TB级）、极致响应**的场景                        |
| **Shenandoah**        | 不分代  | 并发压缩、读屏障     | 并发    | 与ZGC类似，**超低延迟**                                                 |

> **关于 STW (Stop-The-World)**：指GC发生时，所有应用线程被暂停。不同收集器，STW的时间长短差异巨大。

---

## 4. 垃圾收集器选择与启用

### 4.1 选择建议

| 场景         | 推荐收集器                               | 说明                        |
| :--------- | :---------------------------------- | :------------------------ |
| **默认优先**   | JVM默认（JDK 8: Parallel / JDK 9+: G1） | 先跑起来，通过监控发现问题后再调优         |
| **通用首选**   | **G1**                              | 内存 > 4-6GB 的现代服务器应用，通用且安全 |
| **极致低延迟**  | **ZGC / Shenandoah**                | 对延迟有极致要求（<10ms），且JDK版本支持  |
| **高吞吐量**   | **Parallel GC**                     | 后台批处理、计算任务，允许较长停顿         |
| **单核/小内存** | **Serial GC**                       | 单核CPU或内存 < 100M 的环境       |
| **性能测试**   | **Epsilon GC**                      | 无操作GC，仅用于性能测试             |

### 4.2 启用方式

在JVM启动参数中通过 `-XX:+Use` 系列参数指定：

| 收集器 | 启用参数 |
| :--- | :--- |
| Serial + Serial Old | `-XX:+UseSerialGC` |
| Parallel Scavenge + Parallel Old | `-XX:+UseParallelGC` |
| ParNew + CMS | `-XX:+UseConcMarkSweepGC` |
| G1 | `-XX:+UseG1GC` |
| ZGC | `-XX:+UseZGC` |
| Shenandoah | `-XX:+UseShenandoahGC` |

---

## 5. 调优关键参数

### 5.1 通用参数

| 参数 | 说明 | 建议 |
| :--- | :--- | :--- |
| `-Xms` | 堆内存初始大小 | 与 `-Xmx` 设为相同 |
| `-Xmx` | 堆内存最大大小 | 生产环境必须设置 |
| `-Xmn` | 新生代大小 | 推荐占堆内存的 **1/3 ~ 1/2** |
| `-XX:MetaspaceSize` | 元空间初始大小 | 根据类加载数量设置 |
| `-XX:MaxMetaspaceSize` | 元空间最大大小 | **建议设置上限** |

### 5.2 G1 专属参数

| 参数 | 说明 | 默认值 |
| :--- | :--- | :--- |
| `-XX:MaxGCPauseMillis` | 期望的最大GC停顿时间 | 200ms |
| `-XX:G1HeapRegionSize` | 每个Region的大小 | 自动调整（1MB ~ 32MB） |
| `-XX:G1NewSizePercent` | 新生代初始占比 | 5% |
| `-XX:G1MaxNewSizePercent` | 新生代最大占比 | 60% |

### 5.3 Parallel GC 专属参数

| 参数 | 说明 |
| :--- | :--- |
| `-XX:ParallelGCThreads` | GC并行线程数 |
| `-XX:MaxGCPauseMillis` | 最大GC停顿时间（G1更擅长控制） |

### 5.4 CMS 专属参数

| 参数 | 说明 |
| :--- | :--- |
| `-XX:CMSInitiatingOccupancyFraction` | 触发CMS GC的老年代占用比例 |
| `-XX:+UseCMSCompactAtFullCollection` | 在Full GC后整理内存碎片 |

---

## 6. 总结

理解JVM的GC方式，就是理解从**算法** → **收集器** → **参数调优**的完整过程：

1. **理论是基础**：分代收集理论和标记-清除、复制、标记-整理三种算法是所有GC的基石。
2. **选择是核心**：根据应用对**吞吐量**和**延迟**的要求，选择合适的垃圾收集器是调优的关键。
3. **调优是迭代**：通过设置关键的JVM参数，在测试环境持续监控和调整，找到最优配置。

---

## 附：启动参数示例（G1 收集器）

```bash
java -Xms4G -Xmx4G \
     -XX:+UseG1GC \
     -XX:MaxGCPauseMillis=200 \
     -XX:MetaspaceSize=384M \
     -XX:MaxMetaspaceSize=512M \
     -Xloggc:/var/log/app/gc.log \
     -XX:+PrintGCDetails \
     -XX:+PrintGCDateStamps \
     -jar your-app.jar